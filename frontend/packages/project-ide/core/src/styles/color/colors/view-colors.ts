import { baseBg0 } from './base-colors';

const viewColors = [
  {
    id: 'flowide.color.view.topBar.bg',
    defaults: {
      light: baseBg0.id,
      dark: baseBg0.id,
    },
  },
];

export { viewColors };
