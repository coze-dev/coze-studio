-- Modify "workflow_reference" table
ALTER TABLE `opencoze`.`workflow_reference` DROP COLUMN `space_id`, DROP COLUMN `creator_id`, DROP COLUMN `stage`, DROP COLUMN `updated_at`, DROP COLUMN `updater_id`, MODIFY COLUMN `deleted_at` datetime(3) NULL, ADD COLUMN `referred_id` bigint unsigned NOT NULL COMMENT "the id of the workflow that is referred by other entities", ADD COLUMN `status` tinyint unsigned NOT NULL COMMENT "whether this reference currently takes effect. 0: disabled 1: enabled", DROP PRIMARY KEY, ADD PRIMARY KEY (`id`), DROP INDEX `idx_id_referring_biz_type`, ADD INDEX `idx_referred_id_referring_biz_type_status` (`referred_id`, `referring_biz_type`, `status`), ADD INDEX `idx_referring_id_status` (`referring_id`, `status`), ADD UNIQUE INDEX `uniq_referred_id_referring_id_refer_type` (`referred_id`, `referring_id`, `refer_type`);
